# 변경 이력

이 문서는 배포 버전의 사용자 관점 변경 사항을 기록한다.

## 3.5.1 - 2026-07-27

- 변경 입력에 GitHub·GitLab commit URL이나 SHA가 있으면 날짜와 현재 작업트리 및 관계 복구를
  우회하고, 선택한 프로젝트 경로 안에서 해당 commit의 실제 변경만 정확 범위로 사용
- 명시한 commit이 날짜 밖이어도 사용자 선택을 우선하고, 존재하지 않는 SHA는 비슷한 commit으로
  추측하지 않도록 fail-closed 처리
- 확정 commit의 변경 파일이 24개를 넘더라도 프로그램 정보 manifest는 모두 보존하고 파일 본문만
  별도 컨텍스트 예산에서 축약
- 동일 Git 저장소의 여러 하위 폴더를 분석할 때 관계 복구 한도가 중복 적용되던 문제와 diff
  경로·문맥이 참조 근거로 오인되던 문제 수정
- 날짜 의미 복구와 참조 관계 복구가 각각 한도를 소비해 최대 두 배까지 늘어날 수 있던 범위를
  저장소당 하나의 전역 보완 예산으로 통합
- 과거·병합 commit은 현재 checkout이 아닌 선택 시점의 소스와 first-parent diff를 사용하고,
  같은 파일의 다중 commit은 입력 순서가 아니라 조상 관계상 최종 상태·snapshot·diff를 우선
- 대규모 명시 commit에서 한 프로젝트 폴더가 상세 본문 예산을 독점하지 않도록 동일 우선순위
  안에서 선택한 분석 폴더별로 순환 배분
- `.env`, 인증서·개인키 등 민감 파일은 일반 파일명으로 rename된 경우에도 본문과 diff를
  제외하고, 대형 과거 blob과 diff는 메모리 상한을 두어 처리
- commit 제목 형식으로 지정한 단일 범위는 인접 이력을 승격하지 않고, 불완전한 수동 경로와
  모호한 입력의 다중 직접 참조 복구는 유지
- 실제 회귀 입력에서 19개 확정 변경에 과거 commit 24개가 더해져 43개가 되던 범위를
  정확한 19개로 교정하고, 60개 날짜·입력·잡음 시나리오에서 회수율 100%와 변경 오탐 0건 검증

## 3.5.0 - 2026-07-27

- 입력 날짜가 부정확하거나 일부 파일만 적힌 경우 실제 Git 변경과 명시적 참조 및 변경 시점
  근거가 함께 확인된 파일만 분석 범위로 복구
- 경로 없이 파일 종류만 적은 입력은 같은 업무 식별자를 공유하는 응집된 다중 파일 커밋
  하나만 보완 앵커로 선택하고 단일·경합 후보는 자동 추측하지 않도록 제한
- 수동 파일 경로가 정상 매칭되면 관련도가 낮은 임의 Git 커밋을 변경 목록에서 제외
- Java, Python, TypeScript와 SQL의 역참조 복구 및 무관 메서드·미변경 파일 오탐 방지
- 잡음 소스 최대 360개와 입력·날짜 조합 60개 로컬 Git 시뮬레이션에서 실제 변경 회수율
  100%, ServiceImpl 인식률 100%, 무관 변경 0개 검증

## 3.4.1 - 2026-07-24

- 폴더형 실행 파일만 따로 배포할 때 `python313.dll`을 찾지 못하는 문제를 예방하도록 버전형 전체 ZIP 자동 생성
- 배포 폴더와 실행 안내 파일에 `_internal` 필수 동봉 조건 명시

## 3.4.0 - 2026-07-23

- Python FastAPI와 Flask, JSP, 일반 SQL 및 PL/SQL, SAP ABAP와 CDS 및 HANA 소스의 인덱싱과 연관 관계 분석 추가
- Rust, PHP, Ruby, Go 참조 및 안전 검증된 사내 전용 소스 확장자 설정 지원
- 다중 점 파일명의 업무 식별자 손실, SQL 더미 객체 과연결, SQL 확장자 분류 불일치와 민감 파일 판정 오류 수정
- 코드 펜스와 설명이 감싼 단일 JSON, 자연스러운 문장 종결, 중복 항목과 선택 메타데이터의 안전한 자동 정리
- 문체와 절차 수 차이 및 서술문 예시 값은 품질 검토 대상으로 전환하고 근거 없는 실행 대상 ID와 필수 JSON 구조는 계속 차단
- 릴리즈 노트 응답도 코드 펜스와 앞뒤 설명에서 단일 JSON 객체를 안전하게 추출
- 품질 기준을 충족한 초안의 불필요한 AI 재검토 생략과 교정 프롬프트 크기 제한
- provider User-Agent 버전 연동 및 사내용 EXE의 선택 Qwen provider 인증 설정 검증 수정
- 백신·EDR 환경에서 one-file `_MEI` 정리 경고가 반복될 때 사용할 공개·사내용 폴더형 배포 빌드 추가

## 3.3.4 - 2026-07-23

- Windows one-file 종료 시 `_MEI` 임시 폴더 정리 경고 대응
- PyInstaller 6.21.0 고정 및 UPX 비활성화로 사내 백신 환경 호환성 개선

## 3.3.3 - 2026-07-23

- 자연스러운 한국어 서술형 expectedResult 허용
- 글머리표, 줄바꿈, 연속 공백과 마지막 마침표의 안전한 자동 정리
- 릴리즈 노트 뷰 닫기 버튼 폰트 크기 통일

## 3.3.2 - 2026-07-23

- Promptcase Studio 제품 버전 관리의 기준 버전 설정
- CLI 시작 화면과 Windows EXE 파일 속성의 제품 버전 연동
- 제품 버전과 프롬프트 번들 버전의 동기화 검사 도입
- 릴리즈 노트 JSON 외곽 중괄호의 단순 중복·누락 자동 복구
