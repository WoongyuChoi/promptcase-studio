# 변경 이력

이 문서는 배포 버전의 사용자 관점 변경 사항을 기록한다.

## 3.9.1 - 2026-07-28

- 진행률 숫자 툴팁을 별도 말풍선 구현에서 기존 커스텀 툴팁의 값 전용 모드로
  통합해 도움말 툴팁과 동일한 화살표·카드·그림자·접합부 처리를 사용
- 화살표와 둥근 카드 사이의 경계선을 흰색 연결 면으로 덮어 하나의 말풍선처럼
  자연스럽게 이어지도록 수정
- 실제 렌더링 확인과 접합부 회귀 검사를 포함한 외부 API 없는 자동 테스트
  294개 통과

## 3.9.0 - 2026-07-28

- 메인 창을 최대화하거나 세로로 확대하면 왼쪽 제어 패널의 다른 구성요소는
  기존 크기를 유지하고 `4. 변경 내용 및 의뢰서` 입력 영역만 남는 공간만큼 확장
- 기준 1366×768에서는 기존 입력 높이를 유지하고 작은 해상도에서는 기존처럼
  세로 스크롤을 제공해 입력 및 하단 버튼 접근성 보존
- PROGRESS 영역에 마우스를 올리면 기본 툴팁 대신 기존 디자인과 일치하는 작은
  커스텀 말풍선으로 현재 진행률 숫자만 `16%` 형식으로 표시
- 툴팁을 띄운 상태에서도 진행 값이 바뀌면 퍼센트 숫자가 즉시 갱신되도록 구현
- 기준·최대화·소형 해상도 레이아웃과 `16% → 92%` 실시간 툴팁 갱신을 포함한
  외부 API 없는 자동 테스트 294개 통과

## 3.8.0 - 2026-07-28

- 메인 화면에서 단위테스트 템플릿을 저장하면 테스트케이스 다운로드와 동일하게
  저장된 파일의 폴더를 자동으로 열도록 개선
- 릴리즈 노트 뷰의 복사 버튼 왼쪽에 아이콘이 포함된 `릴리즈 노트 다운로드`
  버튼을 추가하고, 팝업에서 수정한 현재 제목과 본문을 UTF-8 TXT로 저장
- 테스트케이스 파일명의 시스템명과 실행 시각을 이어받아
  `시스템명_릴리즈노트_YYYYMMDD_HHMMSS.txt` 형식으로 기본 파일명 생성
- TXT 저장 완료 후 저장 폴더를 자동으로 열고, 기존 파일이 있어도 임시 파일을
  거쳐 안전하게 교체하도록 구현
- 버튼 글자·아이콘·간격과 잘림 여부, 동적 파일명, 편집 문안 저장, 폴더 열기와
  메인 창 연동을 포함한 외부 API 없는 자동 테스트 292개 통과

## 3.7.1 - 2026-07-28

- 테스트 절차, 테스트 데이터와 예상결과에서 내부 시나리오 유형과 제목을 제거하고
  사람이 작성한 문서처럼 번호와 실제 내용만 표시하도록 Excel 매핑 개선
- 시나리오마다 반복하던 비고를 전체 테스트 결과를 종합하는 한 문장으로 통합
- 테스트 데이터가 실제 입력값, 빈 값과 경계 조건 대신 포괄적인 데이터명이나
  클래스·컴포넌트 설정명으로 작성되면 필수 품질 문제로 판정하도록 보완
- `px`, `rem`, `minmax`와 CSS 속성이 사용자용 절차와 예상결과에 포함되면
  응답 계약에서 교정하고, 순수 화면 변경은 잘림·겹침·정렬 같은 관찰 결과로 표현
- 업무 데이터와 계산·저장 결과, 입력 오류와 경계 분기를 순수 스타일 변경보다
  우선해 테스트 시나리오를 선정하도록 생성 및 품질 검토 프롬프트 개선
- 실제 3.7.0 산출물의 포괄적 테스트 데이터 3건과 `88px`·`164px` 예상결과를
  차단하는 회귀 검증을 추가하고 외부 API 없는 자동 테스트 289개 통과

## 3.7.0 - 2026-07-28

- 하나의 정상 흐름을 여러 단계로 나누던 테스트 문안을 입력 조건, 실행, 예상결과와
  판정 기준이 한 묶음인 독립 시나리오 구조로 개편
- 기본적으로 근거가 다른 시나리오 약 3개를 작성하되 1~5개 범위에서 조정하고,
  정상 케이스는 반드시 포함하면서 실패·경계·권한·오류·회귀 케이스는 실제 소스 분기가
  확인될 때만 추가하도록 개선
- 구체적인 날짜, 코드, 금액과 비율은 선택된 diff·소스·fixture에 등장하는 값만 테스트
  데이터로 허용하고 계산된 파생값은 예상결과로 분리
- Excel 테스트케이스의 절차, 테스트 데이터, 예상결과와 비고에 동일한 번호·시나리오
  유형·제목을 표시하고, 최대 5개 문안도 잘리지 않도록 행 높이를 자동 확장
- 의뢰서 문구만으로 만들어진 선택적 실패 시나리오는 소스 근거 부족 품질 문제로 판정하고,
  서로 다른 시나리오의 절차와 결과를 교차 결합해 커버리지로 인정하지 않도록 보완
- 새 응답 스키마 3.0.0과 기존 3.6.x 평면 응답의 읽기 호환성을 함께 제공
- 외부 API를 호출하지 않는 자동 테스트 285개와 Excel PDF 출력 및 OOXML 산출물 검증 통과

## 3.6.3 - 2026-07-28

- 최종 테스트 절차와 결과 확인이 각각 5건인 경우 릴리즈 노트의 확인 요청 10건을
  정상 허용해 불필요한 재시도와 fallback 전환 방지
- Qwen·Gemini가 목록 기호, `[릴리즈]` 제목, 연락 안내 위치 또는 부가 JSON 필드를
  조금 다르게 반환해도 근거 문안은 유지하면서 로컬에서 안전하게 형식 정규화
- 확인 요청이 12건을 넘으면 첫 흐름과 마지막 결과를 보존해 최대 12건으로 정리하고,
  연락 안내가 누락된 경우 표준 안내 문구를 자동 보완
- 정규화가 적용되면 `[NORMALIZE]` 로그를 남기고, 복구할 수 없는 JSON·필수 내용
  오류에만 API 재시도를 사용하도록 개선
- 기존 실패 응답 6건 재검증 및 자동 테스트 265개 통과

## 3.6.2 - 2026-07-27

- 입력과 일치하는 고신뢰 단일 Git 커밋이 선택되면 변경 입력의 `feat:` 접두사 유무와
  관계없이 인접 날짜의 과거 커밋을 확정 변경으로 추가하지 않도록 수정
- 입력 날짜가 어긋나도 변경 문안과 diff가 직접 일치하는 Git 커밋을 찾으면 해당 커밋
  경계만 사용하고 같은 날의 다른 관련 커밋까지 연쇄 확장하지 않도록 보완
- Git 변경 수집을 끈 상태에서 수정일 후보가 많으면 변경 입력과 같은 코드 구간에 근거가
  집중된 파일만 확정 변경으로 선택하고 나머지는 실제 변경으로 단정하지 않도록 개선
- 저장·변경·알림·권한·조회 등 한글 변경 표현과 일반적인 영문 코드 식별자를 연결해
  언어가 다른 입력과 소스도 의미 기반 수정일 선별에 사용할 수 있도록 보완
- 확정 변경에서 제외된 파일이라도 선택 파일의 정확한 import·참조 관계와 변경 의미가
  일치하면 AI 분석 문맥으로 유지해 핵심 유틸리티와 서비스 근거의 누락 방지
- 실제 재현 입력에서 Git 사용 범위는 31개에서 선택 커밋의 19개로, Git 미사용 수정일
  범위는 23개에서 직접 근거 2개와 연관 문맥으로 교정
- 자동 테스트 261개와 입력 품질·날짜 오입력·다중 언어·잡음 저장소 60개 시뮬레이션에서
  오류 및 확정 변경 오탐 0건, 변경 회수율과 ServiceImpl 인식률 100% 검증

## 3.6.1 - 2026-07-27

- 3.6.0의 예시 도메인명이 다운로드 파일명과 Excel `구분`에 강제 적용되던 문제 수정
- 문서 시스템명은 기본적으로 비워 두고 의뢰서의 명시적 시스템명, 근거가 확인된 AI 선택,
  프로젝트 루트 순으로 자동 판정하며 사용자가 직접 입력한 경우에만 고정
- 3.6.0에서 로컬 설정에 자동 저장된 도메인 기본값은 업그레이드 시 자동 감지 상태로 마이그레이션
- 서로 다른 시스템 의뢰, 명시적 시스템명, 다중 시스템 의뢰, 화면명 오인과 프로젝트명
  fallback을 독립 회귀 테스트로 검증

## 3.6.0 - 2026-07-27

- 환경설정에 `기본 시스템명`을 추가하고 다운로드 파일명, 구조화 문서 제목과 Excel 프로그램
  정보의 `구분`이 AI의 화면명 추측 대신 이 값을 공통으로 사용하도록 변경
- Qwen·Gemini가 JSON 문자열 안에 실제 줄바꿈이나 탭을 간헐적으로 반환해도 구조를 추측하지
  않는 안전한 범위에서 복구해 불필요한 전체 응답 재시도를 줄임
- 테스트 데이터는 `다`로 끝나는 문장뿐 아니라 코드·기준년월 등 의미 있는 값 목록도 허용하고,
  실행 절차와 기대 결과의 완결 문장 검증은 그대로 유지
- Git 저장소가 아니거나 `Git 변경 포함`을 끈 경우 수정일과 수동 변경 경로로 분석하고 있음을
  로그와 실행 경고에 표시하며, 삭제 파일은 수동 입력이 필요하다는 제한을 명확히 안내
- 비-Git 수정일 탐색, 수동 삭제 입력, Git 명령 미호출, 시스템명 우선순위와 비정상 JSON
  제어문자 복구를 자동 회귀 테스트로 검증

## 3.5.1 - 2026-07-27

- 변경 입력에 GitHub·GitLab commit URL이나 SHA가 있으면 날짜와 현재 작업트리 및 관계 복구를
  우회하고, 선택한 프로젝트 경로 안에서 해당 commit의 실제 변경만 정확 범위로 사용
- 명시한 commit이 날짜 밖이어도 사용자 선택을 우선하고, 존재하지 않는 SHA는 비슷한 commit으로
  추측하지 않도록 fail-closed 처리
- 확정 commit의 변경 파일이 24개를 넘더라도 프로그램 정보 manifest는 모두 보존하고 파일 본문만
  별도 컨텍스트 예산에서 축약
- 동일 Git 저장소의 여러 하위 폴더를 분석할 때 관계 복구 한도가 중복 적용되던 문제와 diff
  경로·문맥이 참조 근거로 오인되던 문제 수정
- 날짜 의미 복구와 참조 관계 복구가 각각 한도를 소비해 최대 두 배까지 늘어날 수 있던 범위를
  저장소당 하나의 전역 보완 예산으로 통합
- 과거·병합 commit은 현재 checkout이 아닌 선택 시점의 소스와 first-parent diff를 사용하고,
  같은 파일의 다중 commit은 입력 순서가 아니라 조상 관계상 최종 상태·snapshot·diff를 우선
- 대규모 명시 commit에서 한 프로젝트 폴더가 상세 본문 예산을 독점하지 않도록 동일 우선순위
  안에서 선택한 분석 폴더별로 순환 배분
- `.env`, 인증서·개인키 등 민감 파일은 일반 파일명으로 rename된 경우에도 본문과 diff를
  제외하고, 대형 과거 blob과 diff는 메모리 상한을 두어 처리
- commit 제목 형식으로 지정한 단일 범위는 인접 이력을 승격하지 않고, 불완전한 수동 경로와
  모호한 입력의 다중 직접 참조 복구는 유지
- 실제 회귀 입력에서 19개 확정 변경에 과거 commit 24개가 더해져 43개가 되던 범위를
  정확한 19개로 교정하고, 60개 날짜·입력·잡음 시나리오에서 회수율 100%와 변경 오탐 0건 검증

## 3.5.0 - 2026-07-27

- 입력 날짜가 부정확하거나 일부 파일만 적힌 경우 실제 Git 변경과 명시적 참조 및 변경 시점
  근거가 함께 확인된 파일만 분석 범위로 복구
- 경로 없이 파일 종류만 적은 입력은 같은 업무 식별자를 공유하는 응집된 다중 파일 커밋
  하나만 보완 앵커로 선택하고 단일·경합 후보는 자동 추측하지 않도록 제한
- 수동 파일 경로가 정상 매칭되면 관련도가 낮은 임의 Git 커밋을 변경 목록에서 제외
- Java, Python, TypeScript와 SQL의 역참조 복구 및 무관 메서드·미변경 파일 오탐 방지
- 잡음 소스 최대 360개와 입력·날짜 조합 60개 로컬 Git 시뮬레이션에서 실제 변경 회수율
  100%, ServiceImpl 인식률 100%, 무관 변경 0개 검증

## 3.4.1 - 2026-07-24

- 폴더형 실행 파일만 따로 배포할 때 `python313.dll`을 찾지 못하는 문제를 예방하도록 버전형 전체 ZIP 자동 생성
- 배포 폴더와 실행 안내 파일에 `_internal` 필수 동봉 조건 명시

## 3.4.0 - 2026-07-23

- Python FastAPI와 Flask, JSP, 일반 SQL 및 PL/SQL, SAP ABAP와 CDS 및 HANA 소스의 인덱싱과 연관 관계 분석 추가
- Rust, PHP, Ruby, Go 참조 및 안전 검증된 사내 전용 소스 확장자 설정 지원
- 다중 점 파일명의 업무 식별자 손실, SQL 더미 객체 과연결, SQL 확장자 분류 불일치와 민감 파일 판정 오류 수정
- 코드 펜스와 설명이 감싼 단일 JSON, 자연스러운 문장 종결, 중복 항목과 선택 메타데이터의 안전한 자동 정리
- 문체와 절차 수 차이 및 서술문 예시 값은 품질 검토 대상으로 전환하고 근거 없는 실행 대상 ID와 필수 JSON 구조는 계속 차단
- 릴리즈 노트 응답도 코드 펜스와 앞뒤 설명에서 단일 JSON 객체를 안전하게 추출
- 품질 기준을 충족한 초안의 불필요한 AI 재검토 생략과 교정 프롬프트 크기 제한
- provider User-Agent 버전 연동 및 사내용 EXE의 선택 Qwen provider 인증 설정 검증 수정
- 백신·EDR 환경에서 one-file `_MEI` 정리 경고가 반복될 때 사용할 공개·사내용 폴더형 배포 빌드 추가

## 3.3.4 - 2026-07-23

- Windows one-file 종료 시 `_MEI` 임시 폴더 정리 경고 대응
- PyInstaller 6.21.0 고정 및 UPX 비활성화로 사내 백신 환경 호환성 개선

## 3.3.3 - 2026-07-23

- 자연스러운 한국어 서술형 expectedResult 허용
- 글머리표, 줄바꿈, 연속 공백과 마지막 마침표의 안전한 자동 정리
- 릴리즈 노트 뷰 닫기 버튼 폰트 크기 통일

## 3.3.2 - 2026-07-23

- Promptcase Studio 제품 버전 관리의 기준 버전 설정
- CLI 시작 화면과 Windows EXE 파일 속성의 제품 버전 연동
- 제품 버전과 프롬프트 번들 버전의 동기화 검사 도입
- 릴리즈 노트 JSON 외곽 중괄호의 단순 중복·누락 자동 복구
